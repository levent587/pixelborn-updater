{
  "name": "pixelborn-updater",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build --compile --target=bun-windows-x64-baseline index.ts --outfile pixelborn-updater.exe",
    "start": "bun run server.ts"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@octokit/rest": "^22.0.0",
    "@types/adm-zip": "^0.5.7",
    "@types/cli-progress": "^3.11.6",
    "adm-zip": "^0.5.16",
    "cli-progress": "^3.12.0",
    "hono": "^4.7.11"
  }
}
