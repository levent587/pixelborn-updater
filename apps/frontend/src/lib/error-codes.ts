export const ERROR_CODES = {
  GET_LATEST_GAME_VERSION: "Failed to fetch latest game version",
  GET_IMAGE_ZIP_HASH:
    "Failed to fetch latest image version for the selected language",
} as const;
